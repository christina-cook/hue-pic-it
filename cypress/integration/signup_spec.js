describe('Signup page', () => {
  const baseURL = 'http://localhost:3000/'
  beforeEach(() => {
    cy.visit(baseURL)
  })

  it('Should display a header at the top of the page with app name and menu', () => {

  })

  it('Should display a footer at the bottom of the page with links to about section and GitHub accounts', () => {

  })

  it('Should display a signup form', () => {

  })
})
